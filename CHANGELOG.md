# 3AS 版本历史

---

## Alpha 0.001a (2026-08-05) — 版本号规范切换 + Editor Mode 设计立项

**版本号规范变更**：从 SemVer（v0.1.x）切换为 `Alpha 0.0XXa` 序列，反映项目仍处于早期未定型阶段。之前的 v0.1.0/v0.1.1 历史条目保留不改，作为该规范切换前的记录。

**Editor Mode 设计立项**：整理了 GLB 编辑/另存为、材质清理工具、层级/组/Instance、材质与 UV 编辑器、测量包围盒、测量基点系统、操作脚本重放等一整套「查看/编辑模式」需求，写入 [`Doc/EDITOR-SPEC.md`](./Doc/EDITOR-SPEC.md)（详见 [`Doc/INDEX.md`](./Doc/INDEX.md) 的文档组织规范）。这是「Chengduhuagao 案例未来并入 3AS 查看/编辑模式」这个方向确立后的具体规格——本次只整理设计与 todo，未开始实现。

---

## v0.1.1 (2026-08-04) — 真实样品验证修复

用 3ds Max（Autodesk ATF → GLTFConsumer）导出的真实 GLB（`glb/0A_JIMMYCHOO_CD260804.glb`，17 材质全部 `fallback Material`、0 贴图、部分 mesh 多 primitive 多材质 ID）实测后发现并修复：

### 修复
- **模型块表 T/R/S 改为世界空间**：此前只分解节点自身 local matrix，对「网格节点 + 无名父级壳承载真实摆位」这种常见 CAD 导出结构会显示恒等/占位数值（如本例中全部节点显示 S=0.001 且 T=0,0,0）。现在沿父链累乘后再分解，数值才是查看器实际会用到的摆位
- **模型块表新增「材质」列**：显示该节点各 primitive 引用的材质索引（色块+编号）。此前完全没有节点↔材质的映射，对多材质 ID 的 mesh（3ds Max Multi/Sub-Object 材质拆分成多个 primitive）无法判断
- **材质表新增「用于节点」列**：反查每个材质被哪些节点引用，弥补材质名统一为 `fallback Material` 时靠名称无法分辨用途的问题

### 已知限制（新发现，暂未修复）
- 节点注释仍以 `node.name` 作为 localStorage/导出 JSON 的 key，若同一 GLB 内存在重名节点会互相覆盖；本次样品未触发（14 个节点名均唯一）

---

## v0.1.0 (2026-08-04) — 初始版本

### 新增

**核心功能**
- 3D 预览：拖拽旋转/缩放，金色边框包围盒，地面网格辅助
- 拖拽加载：`.glb` 文件拖入视窗即触发预处理
- 示例模型：读取同级 `chengdu-huagao-0801.glb`

**四张表**
- **表1 材质**：PBR 参数（漫反射/金属/粗糙/自发光/高光）、混合模式、贴图槽、可编辑备注
- **表2 贴图**：格式/尺寸（异步解码）/字节数/反向引用、可编辑备注
- **表3 模型块**：顶点/三角面、顶点属性通道、TRS 变换、可编辑显隐/允许/别名/备注
- **表4 场景**：文件信息、包围盒、统计、扩展清单、动画/蒙皮、相机/光照默认配置、整档备注

**注释系统**
- 所有可编辑字段 `onchange` 自动保存到 localStorage
- 键名 `3as:<文件名>:<字节数>`，同文件重开自动恢复注释
- 底部状态栏显示保存时间

**导出**
- 「导出注释 JSON」生成 `.3as.json`，含全部元数据 + 注释
- 符合 SPEC.md v1.0 格式规范

**技术**
- 纯前端单文件（HTML + CSS + JS），无构建步骤
- three.js 0.166.1 本地化 vendor/（脱离 CDN）
- ES 模块 + importmap
- 响应式布局（< 900px 上下分栏）
- 安全：HTML 转义防 XSS，不发送任何外网请求

### 文档
- `README.md`：快速开始、四张表说明、与 Chengduhuagao 案例的关系说明、FAQ
- `SPEC.md`：数据结构规范、扩展支持矩阵、查看器集成代码示例、Phase 2-5 路线图
- `CHANGELOG.md`：本文档

### 已知限制
- 贴图尺寸异步解码，导出时可能仍为 `"…"`
- KTX2 贴图解码失败（需 basis_universal.js polyfill）
- 不支持 glTF 分离格式（.gltf + .bin + 贴图文件夹）
- localStorage 无 LRU 淘汰（容量达限时直接失败）
- 示例模型加载依赖相对路径（需同级目录存在 GLB）

---

## 路线图

| 版本 | 预计时间 | 主要功能 |
|-----|---------|---------|
| v0.2.0 | 2026-09 | 上传端点（Phase 2）、项目 URL 分享 |
| v0.3.0 | 2026-10 | 多文件项目管理（Phase 3）、共享模板 |
| v0.4.0 | 2026-11 | 注释版本控制（Phase 4）、diff 视图 |
| v1.0.0 | 2027-Q1 | 团队协作（Phase 5）、生产就绪 |
