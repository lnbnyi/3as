# 除 Chengduhuagao 案例样品外，其他 GLB 默认视为 WIP / 客户在监修状态，不上传 GitHub。
# 需要本地测试时把文件放进 glb/ 目录即可，此规则会自动排除，不会被 git add 进来。
glb/*
!glb/chengdu-huagao-0801.glb
!glb/.gitkeep

# 开发/测试过程中的临时产物（测试脚本、截图、合成样品 GLB）——不是文档，不进仓库。
# 见 Doc/INDEX.md「开发测试产物放 _dev/」。
_dev/

# 3AS 后端运行时生成的用户上传项目数据（GLB + 注释 JSON + owner token 哈希），
# 跟 glb/* 同一条政策：真实用户数据不上传 GitHub。
server/projects/*
!server/projects/.gitkeep
