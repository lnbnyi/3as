; 部署到 Apache/PHP-FPM 时生效（.user.ini 会被 PHP 自动读取）。
; php -S 内置开发服务器不读取此文件，测试时改用 -d 命令行参数传等价值。
; 代码层面（upload.php）仍然会再做一次 200MB 校验，这里只是让 ini 层不提前拦截合法大小的上传。
upload_max_filesize = 200M
post_max_size = 210M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
