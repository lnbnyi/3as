# 3AS Editor Mode — 实现进度

对应设计文档：[`EDITOR-SPEC.md`](./EDITOR-SPEC.md)。这份清单和 harness 内部的 TaskList 保持同步（内部工具里叫 Task #1-14），每做完一项这里连着内部任务一起勾掉。任务编号后面括号是对应 `EDITOR-SPEC.md` 的章节，方便对照着看设计细节。

- [x] **#1** 确认 EDITOR-SPEC.md 第 11 节的 5 个待定点（§11）—— 2026-08-05 确认完，结论见 §11 决策记录
- [x] **#2** 引入 GLTFExporter，加「另存为 GLB」按钮（§3）—— 2026-08-05 完成并用真实样品（chengdu-huagao-0801.glb）验证通过
- [x] **#3** 加设置按钮 + 附加信息面板（§4、§12 兼容性卡片）—— 2026-08-06 完成。**先按要求检查了有没有残留实现**——搜了"设置"/"settings"/"附加信息"/"兼容性卡片"等关键词，index.html 里只有跟本任务无关的 `#logBtn`/`#logPanel`（那是 #14 诊断日志，已完成的独立功能），确认这次是干净状态，没有可复用的半成品代码。header 左侧新增齿轮图标按钮 `#settingsBtn`（放 `3AS` 标题左侧，右侧已经排了 5 个按钮没有空间），点击弹出居中浮层 `#settingsOverlay`/`#settingsPanel`（跟材质清理菜单/包围盒面板同一套外壳，但内容动态渲染，跟诊断日志面板同一套「打开时渲染」模式）。面板做成开放式「信息卡片」布局（`.info-card`，一张卡片一类信息，方便以后插新卡片），这轮实现一张兼容性卡片：读 `raw.extensionsUsed`，对照内置静态数据结构 `EXT_COMPAT_DB`（照抄 `Doc/EDITOR-SPEC.md` §12 已调研的表格，`KHR_texture_transform`/`EXT_mesh_gpu_instancing`/`KHR_materials_variants`/`KHR_materials_specular`/`KHR_materials_emissive_strength` 五个扩展）列出该文件用到的扩展 + three.js/Babylon.js/model-viewer/Sketchfab 四个查看器的支持情况，矩阵没覆盖到的组合（比如 §12 原文没有逐项调研 Sketchfab）如实标「未查到数据」，不编造。三种状态分别处理：无扩展→「无相关扩展」；有扩展但都不在矩阵里→「未使用已知扩展兼容性数据覆盖的扩展」+ 逐个列出兜底文案；有扩展且部分/全部命中→逐行展示徽章网格。未加载模型时点设置按钮显示「尚未加载模型」提示，不报错不空白。真实样品 `chengdu-huagao-0801.glb`（无 `extensionsUsed`）验证「无相关扩展」分支；手写合成 glTF JSON（`_dev/gen-ext-compat-gltf.js`，`extensionsUsed` 含一个已收录扩展 `KHR_texture_transform` + 一个纯虚构未收录扩展）验证已知扩展徽章渲染正确（three.js/Babylon.js「支持」、Sketchfab 如实「未查到数据」）+ 未知扩展兜底文案分支，两种非空状态都测到了，不只是空状态一种。控制台全程 0 报错。测试脚本：`_dev/test-settings-panel.js`（Playwright，13 项断言全 PASS，可重跑复查），依赖 `_dev/gen-ext-compat-gltf.js`。截图：`_dev/shots/settings-00`（空状态）/`settings-01`（真实样品无扩展）/`settings-02`（合成文件已知+未知扩展混合）。诊断日志按钮（`#logBtn`）暂未并入设置面板——§13 提过这个待办，先保持独立。实现记录见 `Doc/EDITOR-SPEC.md` §4。**同前几轮，harness 的 TaskUpdate 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#4** 材质清理菜单：fallback 重命名 / 黑色材质替色 / 缩放归一化（§5）—— 2026-08-05 完成。材质 Tab 画廊工具栏新增「🧹 清理」按钮，打开批量操作面板：a fallback 占位名（`fallback Material`/`material_N` 类）按引用节点名批量重命名（单引用者→「节点名 材质」，多引用者→「第一个引用者等N个部件」，已有正常名字的不动）；b/c 二选一，只对 baseColor 接近纯黑（RGB<0.05）的材质生效，b 随机浅色（三通道各自独立 200-254 随机取值）/ c 指定统一色（默认 `#c8c8c8`，可改）；d 缩放归一化，把节点局部 scale 烘焙进网格顶点数据（含法线按逆转置矩阵正确变换）、节点 scale 写回 `(1,1,1)`，世界空间外观不变（Apply Scale / Freeze Transform）。a/d 可独立勾选，b/c 互斥，勾选框本身不触发写回，点「应用」才批量执行一次，避免误触。写回跟材质编辑器（#7）同一套双写模式（three.js 实例 + `raw.materials[]`/`raw.nodes[]`），执行完材质画廊/详情/节点表自动刷新，状态栏+日志给量化反馈（如「已重命名 17 个材质，16 个黑色材质已改色」）。**发现上一轮代理已经把这部分代码基本写完**（UI 面板、三个核心函数、测试脚本、6 张截图都已存在，只是文档同步和最终确认没做完就被打断）——这次重新完整读了一遍代码（`isPlaceholderMatName`/`cleanupRenameFallback`/`isBlackMaterial`/`cleanupBlackToColor`/`cleanupNormalizeScale`/`runMaterialCleanup`，含处理"多 primitive 网格内部包装层"、"共享 geometry 被高亮层污染需强制 clone"等细节的注释），代码质量和边界条件处理都合格，判断**直接复用，不重做**，只补上了文档同步（本条 + README.md 表1材质小节 + EDITOR-SPEC.md §5 实现记录）并重新完整跑了一遍验证。真实样品 `chengdu-huagao-0801.glb`（17 材质，16 黑 1 非黑，14 个 scale≠1 节点）验证：a+b 组合全部材质改名不重复、16 个黑色材质变浅色互不相同、非黑材质 #16 未被误改，three.js 实例与 raw 逐值同步，导出 GLB 解析确认材质数/名称/颜色都是清理后新值；单独测 c（改默认色 `#3388ff`）全部黑色材质变成同一指定色、非黑材质未受影响；单独测 d 选中 scale `[0.001,0.001,0.001]` 节点，归一化前后世界空间包围盒尺寸/中心差异在 1e-8 量级（视觉完全一致），节点自身局部 scale 精确变 `(1,1,1)`，导出 GLB 解析确认写回正确；控制台全程 0 报错。测试脚本：`_dev/test-cleanup-menu.js`（Playwright，可重跑复查，全部 PASS），截图 `_dev/shots/cleanup-00` 至 `cleanup-05`。实现记录见 `Doc/EDITOR-SPEC.md` §5。**同前几轮，harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch`（查询 "TaskUpdate TaskGet"）仍无匹配结果，只更新了这份文件**
- [x] **#5** 节点表升级为树状，显示分组节点（§1、§6 前半）—— 2026-08-05 完成，用 chengdu-huagao-0801.glb 真实样品 + 合成两层父子 glTF JSON 验证通过
- [x] **#6** 创建 Instance（方案 ii）+ 导出选中物体（§6 后半）—— 依赖 #1、#5，2026-08-05 完成。节点树每行新增「⧉ Instance」「⇩ 导出」两个操作按钮（点击即对该行节点生效，没有另外做「选中」状态）。创建 Instance：跟材质编辑器同一套双写模式——① 递归复制 raw.nodes[] JSON 记录（叶子节点 mesh 索引原样引用，分组节点子树结构逐层复制），新节点一律过 `_instanceN` 后缀避重名（不只是根节点，子树里每个节点都单独避重）；② three.js 场景图里用 `Object3D.clone(false)` + 手动重建子树（不能直接 `clone(true)`，多 primitive 网格节点内部有 GLTFLoader 自己搭的 Group 包装层，会跟真实 raw 子节点混在一起，需要靠 `gltf.parser.associations` 的 `info.nodes` 字段分辨哪些子对象对应真实节点、哪些是内部包装），geometry/material 全程只传引用不 clone。新根节点局部 translation/matrix 加 X+2 米固定偏移（矩阵型和 T/R/S 型节点分别处理），仅用于视口里肉眼区分新旧两份，没有可视化摆放交互。导出选中：实测验证「直接把子树 Object3D 传给 GLTFExporter.parse()」这个假设成立，不需要手写依赖收集算法——见下方验证记录。用真实样品 chengdu-huagao-0801.glb 验证通过：叶子节点 PArc864 创建 Instance 后 mesh 索引与原节点一致、视口截图确认新增了偏移开的副本几何体；分组节点 node_3（挂 4-primitive 的 L6）创建 Instance 后子树 mesh 序列 `[null,1]` 与原树完全一致、新增节点数精确等于原子树节点数；给新 Instance 挂别名后原节点注释未被覆盖（Playwright 直接验证两个 `nodeAnno` 记录独立）；「另存为 GLB」下载文件解析 JSON chunk 确认新节点出现在导出结果里，且整档文件从 1145028 字节变成 999200 字节（不升反降，证明 mesh 复用没有让顶点数据翻倍）；「导出选中」单个叶子节点导出 GLB 解析后 materials=1/meshes=1（精确匹配预期依赖，不含另外 16 个材质），分组节点导出后 materials=4（精确匹配 L6 四个 primitive 各自的材质，不多不少）；控制台全程 0 报错。测试脚本：`_dev/test-instance-export.js`（Playwright，可重跑复查）。实现记录见 `Doc/EDITOR-SPEC.md` §6。**同前几轮，harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch` 仍搜不到（只返回 TaskStop/SendMessage），只更新了这份文件**
- [x] **#7** 材质编辑器（可写回，非只读）（§7 前半）—— 2026-08-05 完成，材质表拆成色块画廊+详情编辑区两栏（顺带解决 UI 评审提的 11 列裁切密度问题，见 `Doc/2026-08-05-ui-review-panel-density.html` §02），baseColor/金属度/粗糙度/自发光/自发光强度可编辑并同步写回 three.js Material 实例（经 `gltf.parser.associations` 反查全部引用实例）+ `raw.materials[i]`；用真实样品验证（chengdu-huagao-0801.glb，17 材质）：材质 #0「fallback Material」原始纯黑 `[0,0,0,1]`，改成 `#ff2020` 后视口对应网格块实测变红（截图对比）、导出 GLB 手动解析 JSON chunk 确认 `baseColorFactor` 变成 `[1,0.0144,0.0144,1]`（不是原始值）；用于节点反查+跳转定位正常；控制台 0 报错。实现记录见 `Doc/EDITOR-SPEC.md` §7。**harness 的 TaskUpdate/TaskGet 工具本轮再次搜索确认不存在（`ToolSearch` 只返回 TaskStop/SendMessage），只更新了这份文件**
- [x] **#8** 贴图 UV 变换编辑 `KHR_texture_transform`（§7 中段、§12）—— 2026-08-05 完成，材质详情编辑区新增「贴图 UV 变换」分区（移动/缩放/旋转，按材质+贴图槽分组，5 个可能槽位各一组），不用自己解析扩展（GLTFLoader/GLTFExporter 官方已支持，映射到 `THREE.Texture.offset/repeat/rotation`）；处理了两个关键坑：① 未编辑的贴图会被多材质/多槽位共享同一 `THREE.Texture` 对象，首次编辑时才 clone 出独立实例（`ensureIndependentTexture`+`uvTexCache`）；② 金属粗糙槽的 `metalnessMap`/`roughnessMap` 必须共享同一个 clone，否则 `GLTFExporter.buildMetalRoughTexture()` 会在两者不同引用时触发合并重打包丢失变换。用手写合成的棋盘格贴图 glTF（`_dev/gen-checker-gltf.js` → `_dev/test-uv-checker.glb`，不依赖二进制 GLB/canvas/npm 包）截图验证移动（色块对调）/缩放（8x8→3x3）/旋转（方块变菱形）视觉生效，验证同一贴图的固色槽和自发光槽独立变换互不干扰，导出 GLB 解析 JSON chunk 确认 `KHR_texture_transform` 数值精确写回、全默认值时正确不写扩展字段；真实样品 `chengdu-huagao-0801.glb`（0 贴图）确认合理降级显示「此材质没有贴图槽」不报错。控制台 0 报错。实现记录见 `Doc/EDITOR-SPEC.md` §7。测试脚本：`_dev/test-uv-editor.js`。**同上一轮，harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch` 仍搜不到（只返回 TaskStop/SendMessage），只更新了这份文件**
- [x] **#9** 测量包围盒（OBB）创建/编辑/删除（§7 后半、§8）—— 2026-08-06 完成。模型块树每行新增「⬚ 包围盒」操作按钮，没有 bbox 时点击=按节点**世界空间**旋转角（不是节点自身 local 旋转，复用 `nodeWorldMatrix()`）默认生成一个定向包围盒并打开编辑面板，已有 bbox 时点击=直接打开编辑（不重新生成，不冲掉用户手动改过的角度）。算法是标准的"给定朝向求该朝向下最小包围盒"三步手法：子树全部网格顶点变换到世界空间后乘 R 的逆旋转对齐到局部空间→在对齐空间里求 AABB→中心乘回 R 变换回世界空间（尺寸不用变换，本来就是沿 OBB 自己局部轴的量纲）。视觉上 `BoxGeometry`+`EdgesGeometry` 12 条棱边虚线框，颜色用青色 `0x39d6ff`（2026-08-05 #25 统一节点选中/材质高亮成黄色系之后空出来的色相，这里复用，跟节点选中黄实线框/材质高亮黄叠加/场景整体金色边框三种都拉开视觉区分）。编辑：面板三个角度数字输入框改完自动重算 size/center 并重画线框，附「重置为节点世界旋转角」按钮。删除：清掉 `nodeAnno().bbox` 并重画（线框消失）。数据结构严格对齐 `Doc/EDITOR-VIEWER-CONTRACT.md` 第六节 `BoundingBoxAnnotation { rotationDeg, size, center }`，没有另外发明字段名，直接存进 `nodeAnno(name).bbox`，导出注释 JSON 自然带出去。场景整体包围盒新增「手动指定」开关（`anno.sceneBbox = { manual, size, center }`，字段补进 `SPEC.md` 的 `Annotations` 接口），勾选后 6 个数字输入框（米）覆盖显示，取消恢复自动计算的 mm 格式只读值。**发现这是上一轮代理已经写完的功能**——这次任务开始时 `index.html` 里 UI 面板/CSS/`computeOBB`/`buildBBoxHelper`/`syncBBoxHelpers`/`generateBBox`/`openBBoxPanel` 等核心函数、以及测试脚本 `_dev/test-bbox.js`+6 张截图都已经存在，只是文档同步没做完（`EDITOR-SPEC.md`/`EDITOR-VIEWER-CONTRACT.md`/`SPEC.md`/`README.md`/本文件都还没更新，本条也还是未勾选），像是文档收尾阶段被打断——逐行读完代码判断质量合格、边界条件（空分组节点没有网格顶点时 `computeOBB` 正确返回 `null` 而不是崩溃）处理到位，**直接复用，不重做**，重新完整跑了一遍 `_dev/test-bbox.js` 确认在当前代码状态下仍然正确工作（30 个断言全部 PASS），然后补上了这次要求的全部文档同步。真实样品 `chengdu-huagao-0801.glb` 验证：选中一个世界旋转角非零的节点（`node_1`，`[0.6°,-7.4°,0.1°]`）生成包围盒，线框朝向跟节点旋转对齐（`helper.quaternion` 跟节点世界旋转角 `angleTo` 误差 <1e-4 rad，不是简单轴对齐盒子，截图 `_dev/shots/bbox-02-wireframe-in-viewport.png` 肉眼可见线框贴着倾斜的文字网格倾斜）；手动改旋转角到 Z=45° 后 size/center 数值确实重新计算（`3.293×1.229×0.031m`→`2.516×2.368×0.031m`）且线框跟着重新画出新朝向、旧线框不残留；删除后线框数量归零、节点行按钮 `has-bbox` 标记摘掉；导出注释 JSON 解析确认该节点注释含 `bbox.rotationDeg`/`.size`/`.center` 三个长度 3 数组，字段名跟契约一致；场景整体包围盒手动覆盖填入 `尺寸X=12.345m`/`中心Y=3.21m`，导出 JSON 确认 `scene['包围盒尺寸']` 变成对应 mm 值（`12345mm`）、`annotations.sceneBbox.manual===true`，取消勾选后场景表精确恢复原始自动值；控制台全程 0 报错。测试脚本：`_dev/test-bbox.js`（Playwright，可重跑复查，30 个断言全 PASS），截图 `_dev/shots/bbox-00` 至 `bbox-05`。实现记录见 `Doc/EDITOR-SPEC.md` §7 末尾（新增独立小节）。**同前几轮，harness 的 TaskUpdate/TaskGet 工具本轮再次 `ToolSearch`（查询 "TaskUpdate TaskGet"）确认无匹配结果，只更新了这份文件**
- [x] **#10** 测量基点系统（§8）—— 依赖 #5，2026-08-06 完成。基点 `{name, position:[x,y,z], zRotation}` 存进场景级注释 `anno.basepoints`（数组，允许多个），节点关联存 `nodeAnno(name).basepointRef`（存基点 name 字符串，改名有级联同步，撞名自动避重）。**GLB 原生约定这次直接定下来**（`Doc/EDITOR-VIEWER-CONTRACT.md` 第七节「待确认」第 2 条一直没确认，这次按任务要求自己定并回填）：优先级 1 找场景里名字是 `Origin`/`_origin`/`origin`（大小写不敏感）的节点，命中则取它的世界空间 T 当 `position`、世界空间 Y 轴欧拉分量当 `zRotation`（本项目 glTF/three.js 场景是 Y-up，Y 才是真正的竖直轴，"只绕 Z 轴的朝向角"这个字面措辞按语义理解成"绕竖直轴的方位角"，理由见下方文档回填）；真实样品 `chengdu-huagao-0801.glb` 没有这种命名节点，正确降级到优先级 2——用现有场景表已经在算的包围盒中心当 `position`，`zRotation` 默认 0；优先级 3（都没有）由用户在「场景」Tab 新增的基点管理区域手动新增/编辑（内联可编辑名字/位置XYZ/朝向°的列表，「＋新增基点」「⟲生成默认基点」两个按钮）。视口可视化：橙色 `0xff9d2e` 标记（跟节点选中/材质高亮黄、包围盒青、场景金边都拉开色相），`AxesHelper`+`ArrowHelper`（强调朝向）+贴地半透明圆环三件套，只 `scene.add()` 不进 `model` 子树。模型块树新增「关联基点」下拉框 + 「相对基点坐标」只读展示两列，留空关联时 fallback 到场景第一个基点（而不是显示空白），相对坐标计算复用 `nodeObjects`/`computeOBB` 同一套世界空间变换路线（节点世界坐标减基点位置、再乘基点朝向逆旋转），没有另起一套变换逻辑。真实样品验证：默认基点精确等于 `tables.sceneAutoBbox.center`（优先级 2 生效，容差覆盖 4 位小数舍入）；新建基点手动改 position/zRotation 后视口标记位置/朝向四元数同步重算（误差 <1e-6 rad）；节点关联新基点后"相对基点坐标"用**独立于被测函数的手写三角公式**重新算一遍逐分量核对一致，另外验证了"基点设在节点世界坐标上、zRotation=0 时相对坐标精确为 [0,0,0]"这个简单场景的手动验算；取消关联正确 fallback。手写合成 glTF JSON（含大小写混合的 `Origin` 命名节点）单独验证优先级 1：`zRotation` 精确等于构造时写入的 30°，`position` 通过独立反查该节点 `matrixWorld` 的另一条代码路径交叉核对完全一致；UI「⟲生成默认基点」按钮手动触发同样正确命中、重名自动改「默认基点2」。导出注释 JSON 解析确认 `annotations.basepoints` 数组 + `annotations.nodes[name].basepointRef` 字段都在。控制台全程 0 报错。测试脚本：`_dev/test-basepoints.js`（Playwright，可重跑复查），依赖 `_dev/gen-origin-node-gltf.js`。截图：`_dev/shots/bp-00` 至 `bp-04`。文档同步：`SPEC.md`（`Annotations`/`MeasurementBasepoint` 接口）、`Doc/EDITOR-VIEWER-CONTRACT.md`（第五节回填 GLB 约定细节 + zRotation 轴取舍说明，第七节「待确认」第 2 条标记已回填）、`Doc/EDITOR-SPEC.md`（§8 末尾实现记录）、`README.md`（新增「测量基点系统」独立小节 + 表3操作列补充说明）。**同前几轮，harness 的 TaskUpdate 工具本轮 `ToolSearch` 仍搜不到，只更新了这份文件**
- [x] **#11** 滑动条统一暗色风格（§9）—— 2026-08-06 完成，审查全应用范围内所有 `input[type="range"]`（材质详情金属度/粗糙度/自发光强度 + 每贴图槽 UV 变换移动/缩放/旋转），CSS 是裸类型选择器全局唯一一处规则，天然没有遗漏；顺带补上原本缺失的 hover/focus 交互反馈，跟其它表单控件一致
- [x] **#12** 场景菜单 + 模型子菜单（重新载入 GLB）（§10 前半）—— 2026-08-06 完成。**先检查了有没有残留实现**——搜了「场景菜单」/「重新载入」/「reload」/「sceneMenu」/「模型子菜单」等关键词，`index.html` 里全部无匹配，确认是干净状态，这次是从零实现，没有可复用的半成品代码。Header 最左侧新增独立下拉菜单「场景 ▾」（`#sceneMenuBtn`/`#sceneMenuDropdown`），跟右侧那排平铺按钮区分开——点击展开子菜单而不是直接触发动作，菜单里目前只有「模型」一个分组，组内一项「重新载入 GLB」（`#reloadGlbBtn`）。点击后行为按任务要求做成最简单版本：收起菜单 + 触发隐藏的 `#fileInput` 原生选择框，选中文件后走的是已有的 `openBuf()`/`preprocess()` 流程（跟 `#fileInput.onchange` 处理器完全共用，没有另写一套加载逻辑），等同于「打开 GLB」按钮，只是入口挪到场景菜单——完整的新旧模型节点差异对比/操作脚本重放是 #13 的范围，这次不做。菜单展开/收起交互参考现有材质清理菜单/设置面板的「点击外部区域收起」惯例，另外补了 ESC 收起（这两个既有浮层都没有的交互，属于这次新加菜单体系顺手补齐）。**实现过程中发现并修复了一个真实浏览器坑**：Playwright 实测确认原生 `<input type="file">` 重新选中「路径和内容都跟上次一样」的文件时不会触发 `change` 事件，连「同路径但内容字节确实改过」（模拟 3ds Max 覆盖导出同一路径）这种情况在测试环境里也没触发——这正好命中「重新载入 GLB」最主要的目标场景（用户覆盖导出到同一路径），不修的话用户点了感觉像没反应。修复方式是弹出对话框前先清空 `fileInput.value`，「打开 GLB」按钮（`#openBtn`）也一并做了同样修复（同一个 `#fileInput`，同一个坑）。真实样品 `chengdu-huagao-0801.glb` 验证：正常加载一次→编辑材质#0 baseColor（黑色 `[0,0,0,1]`→红色 `#ff2020`）→场景菜单→模型→重新载入 GLB 选**同一个文件**，`window.__debugRaw.materials[0].pbrMetallicRoughness.baseColorFactor` 精确恢复到原始 `[0,0,0,1]`（不是保留编辑后的红色，证明是全新 `preprocess()` 而不是恢复缓存状态），材质/贴图/节点块三项统计跟首次加载一致；菜单展开/点外部收起/ESC 收起三种交互都测到；重跑既有回归测试 `test-cleanup-menu.js`/`test-bbox.js`/`test-basepoints.js` 确认 `fileInput.value` 清空这个改动没有破坏其它依赖文件加载流程的既有功能，全部 PASS；控制台全程 0 报错。测试脚本：`_dev/test-scene-menu.js`（Playwright，可重跑复查，全部 PASS）。截图：`_dev/shots/scenemenu-00` 至 `scenemenu-03`。实现记录见 `Doc/EDITOR-SPEC.md` §10。**同前几轮，harness 的 TaskUpdate 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#13** 操作脚本记录 + 重放系统（§10 后半，含差异梳理 UI）—— 依赖 #1、#4、#6、#7、#8、#9、#10，2026-08-06 完成，**本轮开发的最后一个任务**。**开工前先按要求检查了有没有残留实现**——搜了「操作脚本」/`script`/「重放」/`replay`/`recordScriptOp`/`anno.script`（注意跟 `_dev/` 测试脚本文件名里的"script"一词区分开），`index.html` 里全部无匹配，确认是干净状态，从零实现。严格复用 #22 撤销系统（`pushUndo`）已经接入的那批写回位置，没有重新埋点——材质字段编辑、材质清理菜单批量操作、创建 Instance、节点移动/旋转、贴图上传/替换/移除、UV 变换，六类跟 #22 名单逐条对应；新增 `recordScriptOp(op, target, params)` 在同一批调用点紧挨着 `pushUndo` 一起调，记的是"这次操作是什么"而不是"怎么撤销"。脚本存进 `anno.script`（`{op, target, params, ts}` 数组），导出注释 JSON 自然带出去，不用额外写导出代码。「场景 ▾ → 模型 → 重放操作脚本」：差异梳理（按 `(kind,byName)` 分组比对脚本引用过的节点/材质名单 vs 当前模型实际名单，泛化到同时覆盖节点和材质两种目标，比任务原文字面只提"节点"做得更完整）+ 三选项（保留=这次跳过不删记录；放弃=永久删除；重新指定=下拉选新目标，就地更新且持久生效）+ 按脚本原始顺序逐条重放（直接复用 `setMatField`/`setUvField`/`applyNodeWorldTransform`/`createInstance`/`uploadMatTexture`/`removeMatTexture`/`runMaterialCleanup` 等既有写回函数，没有另写一套重放专用逻辑）+ 完成后总结成功/跳过/失败条数，每一步都进诊断日志不静默。**开发过程中用真实样品测试贴图重放时发现一个真实 bug 并修复**：`chengdu-huagao-0801.glb` 清理前全部 17 个材质都叫同一个名字「fallback Material」，纯按名字匹配会让材质相关操作全部撞车解析成材质 #0——加了 `target.atIndex`（记录时的索引，当"同名候选优先提示"用，不是主键）兜底修复，索引对不上才退回按名字找第一个。**这次没做的部分（如实列出）**：①「从外部注释 JSON 文件导入 script 应用到全新模型」这个更完整的流程没做（任务原文允许的简化范围），当前只做"对当前已加载模型重放当前 anno.script"，"重新载入GLB选同一文件脚本自动接上"靠的是 localStorage 按 fileKey 持久化这个既有机制，不是专门写的导入功能；②「保留」选项的"以后同名节点出现自动接上"是被动的，没有主动监听/后台扫描；③材质拖拽指定（#19）/包围盒（#9）/测量基点（#10）/场景整体包围盒手动覆盖没有记进脚本，跟 #22 撤销系统的已知限制范围一致。真实样品验证：三步真实 UI 编辑（材质改色/清理菜单/创建Instance）→ 逐条核对 `anno.script` 精确记录 → 导出 JSON 确认 `script` 字段完整带出 → 「重新载入GLB」选同一文件确认运行时状态清空但脚本经 localStorage 保留 → 重放确认三步全部重新执行（材质变红/材质重命名/节点数增加）且重放本身不会自我膨胀脚本（`scriptReplaying` 标记防重复）→ 差异梳理三选项（保留/放弃/重新指定）逐一测试行为符合预期，过程中还发现并妥善处理了"清理菜单重命名导致旧 editMaterial 记录的目标名连带失效"这个真实连带效应；控制台全程 0 报错。测试脚本：`_dev/test-script-replay.js`（44 项断言全 PASS）+ `_dev/test-script-replay-texture.js`（18 项断言全 PASS，专门补测贴图三类操作 + atIndex 修复）。另重跑 `test-undo-status.js`/`test-scene-menu.js`/`test-bbox.js`/`test-basepoints.js`（全 PASS）+ `test-cleanup-menu.js`/`test-instance-export.js`/`test-texture-upload.js`/`test-mat-editor.js`/`test-uv-editor.js`/`test-node-transform.js`（撞上 #21/#22 已记录过的旧 `#exportGlbBtn` 选择器预置问题，撞上之前全部断言 PASS，非本次引入的回归）。文档同步：`README.md`（新增「操作脚本记录 + 重放系统」独立小节 + 更新「场景菜单」小节描述）、`SPEC.md`（`Annotations` 接口新增 `script`/`ScriptEntry`/`ScriptTarget`）、`Doc/EDITOR-SPEC.md`（§10 末尾新增实现记录）。测试钩子：`window.__debugScript`。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#14** 日志系统 + 报错系统（§13）—— 2026-08-05 完成，头部独立「日志」按钮+可展开面板，接入 GLTFLoader 解析失败/警告、贴图解码失败、GLTFExporter 导出失败三处，用真实样品 + 手工截断的损坏 GLB 验证通过（触发真实 RangeError 并在面板中正确显示、可展开看完整堆栈）
- [x] **#15** 选中高亮基础设施：节点选中→视口包围框 / 材质选中→网格高亮（§6.1，为 #9 包围盒、#17 移动旋转打基础）—— 2026-08-05 完成。模型块树点行（或「用于节点」chip 点击）选中节点，视口对应 `Object3D` 出青色 `Box3Helper` 包围框（分组节点自动框住整个子树合并包围盒），跟场景整体金色边框区分；材质画廊点击色块卡片，视口所有引用该材质的网格叠加品红发光层（`matInstances` 反查全部命中网格，一个材质被多网格引用时全部高亮）。两套高亮独立存在、互不清除（用户确认的设计取舍：材质高亮和节点选中互不影响，除非用户主动切换同类型选中），同类型内部切换时旧高亮先清后加，不叠加残留。两套高亮对象都只 `scene.add()`、不进 `model` 子树，天然被 `GLTFExporter` 的导出路径排除，不需要「导出前清理」这种脆弱逻辑；材质高亮也不修改任何材质持久字段（叠加独立 Mesh，不碰 `.emissive`/`.color`）。真实样品 `chengdu-huagao-0801.glb` 验证：节点选中/切换/取消选中/分组节点子树包围框、材质选中命中 2 个网格（`L6`+`Yeang Keat OBE` 共用材质 #1）、材质切换清除残留、「用于节点」chip 点击联动跳转+闪烁+包围框、导出 GLB 解析确认材质数量与 `emissiveFactor`/`baseColorFactor` 跟高亮前逐字节一致（未被污染），控制台 0 报错。测试脚本：`_dev/test-highlight.js`。实现记录见 `Doc/EDITOR-SPEC.md` §6.1。**harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch` 仍搜不到（"TaskUpdate TaskGet" 查询无匹配结果），只更新了这份文件**

- [x] **#16** 材质贴图上传/替换（含空贴图槽新增贴图）—— 2026-08-05 完成。#8 阶段「贴图 UV 变换」分区从「只渲染已有贴图的槽位」改成 5 个可能槽位（固色/金属粗糙/法线/AO/自发光）永远全部渲染，每个槽位一行缩略图+「上传贴图」/「替换贴图」按钮（文案随当前有没有贴图切换）+「移除贴图」按钮（有贴图时才出现）+隐藏 file input；UV 变换滑动条只在该槽位当前确实有贴图时才继续渲染在下方。解码复用贴图表原本就在用的 `createImageBitmap`，不引入新方式；新贴图 `flipY`/`wrapS`/`wrapT`/固色自发光槽的 `SRGBColorSpace` 照抄 `GLTFLoader` 对 glTF 贴图的既有约定；金属粗糙槽两个 three.js 属性天然指向同一个新建 Texture 对象，规避 #8 阶段已知的 `GLTFExporter` 合并重打包坑；替换贴图时继承原槽位已设的 `KHR_texture_transform` 并重新应用到新贴图。写回链路跟材质其它字段同一套模式：① `matInstances` 反查全部 three.js 实例改贴图属性（导出/视口真正数据源）；② `raw.images[]`/`raw.textures[]` 新增记录 + `raw.materials[i]` 对应槽位指向新索引（表格展示/JSON 导出一致性用，新增 `UV_SLOTS[].setJson` setter 跟已有 `jsonPath` getter 成对）。移除只清空槽位引用，不删除 `raw.images`/`raw.textures` 里已有条目本身。贴图表构建逻辑从 `buildTables()` 抽成独立的 `rebuildTexTable()`（跟 `rebuildNodeTable()` 同一个抽出理由），新增模块级 `currentBuf` 供原始内嵌贴图异步解尺寸复用，本工具自己上传的贴图尺寸在上传时就已同步拿到（`raw.images[i]._dims`），不用等异步解码。用真实样品 `chengdu-huagao-0801.glb`（17 材质 0 贴图）验证：材质 #16（浅绿盒子）baseColor 槽上传测试棋盘格图后 three.js `material.map` 实测 `colorSpace:'srgb'`/`flipY:false`/尺寸正确，`raw` 侧引用链路正确；**发现并记录了样品本身的几何限制**——探测确认样品全部 21 个 primitive 都没有 `TEXCOORD_0`，上传贴图后因为没有 UV 肉眼只能看到单一纯色（不是花纹），这是样品限制不是功能问题，为满足"肉眼确认贴图纹理生效"这条验收标准额外生成了一个有真实 UV 展开的合成四边形（`_dev/gen-empty-slot-gltf.js`），上传前纯灰平面、上传后清晰红白棋盘格图案铺满整个面，截图对比确认；「另存为 GLB」下载后手写 GLB chunk 解析确认 `images[]` 条目有 `bufferView` 没有 `uri`（证明贴图数据真的内嵌进 BIN 二进制 chunk），从 BIN chunk 按 bufferView 偏移切出的字节头 8 字节精确匹配 PNG 魔数；替换测试确认 `material.map.uuid` 前后不同（真替换不是叠加）；移除测试确认 `material.map` 变回 `null`；贴图表确认新贴图正确出现；控制台全程 0 报错；重跑 `test-mat-editor.js`/`test-uv-editor.js`/`test-alpha-doubleside.js` 三个既有回归测试确认未破坏材质编辑/UV 变换/透明双面功能（`test-uv-editor.js` 里"槽位数"相关旧断言输出从这轮起变化是 #16 新设计的预期行为，不是回归，UV 变换数值读写本身逐字节验证通过）。测试脚本：`_dev/test-texture-upload.js`（Playwright，可重跑复查），依赖 `_dev/gen-test-textures.js`+`_dev/gen-empty-slot-gltf.js`。实现记录见 `Doc/EDITOR-SPEC.md` §7。**同前几轮，harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch`（查询 "TaskUpdate TaskGet"）仍无匹配结果，只更新了这份文件**
- [x] **#17** 节点移动/旋转基础编辑（视口拖拽 + 数值输入）—— 依赖 #15，2026-08-06 完成（数值输入路径，视口拖拽 gizmo 未做，任务允许先不做）。**先检查了有没有残留实现**——搜了"移动"/"旋转编辑"/"transformEdit"/节点位置相关关键词，`index.html` 里只有第 9 节包围盒的 `rotationDeg`、第 10 节基点的 `zRotation`（都是不同功能，任务已提醒过别搞混）和节点表已有的只读 T/Rdeg 展示，确认这次是干净状态，没有可复用的半成品代码，从零实现。节点行新增第四个操作按钮「✥ 移动/旋转」，弹出跟包围盒面板同一套外壳的编辑面板：6 个数字输入框（世界空间位置 X/Y/Z 米、旋转 X°/Y°/Z° 度）+ 每字段独立「还原到原始值」+ 面板顶部「还原此节点全部变换」整体还原。写回目标是 `raw.nodes[i]`（原来 `matrix` 表示继续写 `matrix`，原来 `translation/rotation/scale` 分开表示继续分开写，不强行转换），不是 viewer 端 `UserScheme.transforms`。核心转换 `localMatrix = inverse(parentWorldMatrix) * targetWorldMatrix`。**实现过程中踩到一个坑并已修复**：3AS 内部存在两套「世界空间」——`nodeWorldMatrix()`（纯 raw.nodes 父链累乘，节点表 T/R 列用这套）vs three.js `obj.matrixWorld`（多套了一层 `preprocess()` 的视口显示归一化缩放/居中）——第一版实现从 `obj.matrixWorld` 取当前世界缩放去拼目标矩阵，真实样品测试时数值对不上（差几十倍，正好是归一化系数），改成全程统一走 `nodeWorldMatrix()` 后误差降到 1e-4 量级浮点噪声，详见 `Doc/EDITOR-SPEC.md` §14。真实样品 `chengdu-huagao-0801.glb` 验证：选了有父节点的叶子节点 `Prof.Jimmy Choo`（父级 `node_1`，典型"网格节点+无名父级壳"结构），移动/旋转后**用测试脚本独立手写的父链累乘重算**（不调用被测函数本身）核对世界坐标误差 <0.0001m/0.001°；节点表 T/R 列同步刷新；单字段还原/整体还原都精确回到原始值；「另存为 GLB」导出后解析局部矩阵跟内存里编辑后的 `raw.nodes[ni]` 逐分量一致；先生成包围盒再改变换，包围盒被自动清除（联动设计取舍：清除+提示重新生成，不做自动跟着重算，避免静默覆盖用户手动调过的角度）；控制台全程 0 报错。测试脚本：`_dev/test-node-transform.js`（Playwright，可重跑复查，全部断言 PASS）+ `_dev/test-node-transform-visual.js`（干净视口截图）。截图：`_dev/shots/transform-00` 至 `transform-06`、`transform-visual-00`/`01`。实现记录见 `Doc/EDITOR-SPEC.md` §14。**同前几轮，harness 的 TaskUpdate 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#18** 材质编辑器补充：透明/双面可编辑 + 滑动条布局收紧 —— 2026-08-05 完成。alphaMode（`OPAQUE`/`MASK`/`BLEND` 下拉框）+ doubleSided（单双面开关）从"只读展示"（这四个字也删掉了，huashu-design 评审提过暴露开发状态的文案不专业）改成可编辑，映射规则照抄 `vendor/loaders/GLTFLoader.js` 的 `createMaterial()` 反着做（BLEND→`transparent:true`+`depthWrite:false`，MASK→`alphaTest`取`alphaCutoff`默认0.5，OPAQUE→都清零；doubleSided→`material.side` DoubleSide/FrontSide），沿用「改字段→two 边同步（three.js 实例 + `raw.materials[i]`）→另存为GLB生效→独立还原按钮」同一套模式；金属度/粗糙度、自发光/自发光强度、透明模式/双面三组滑动条+控件改两两并排（新增 `.mf-row-pair`/`.mf-cell`），不改控件类型，同样字段数纵向占用行数从 6 行收紧到 4 行（新增 `select` 元素暗色主题 CSS，项目里第一个下拉框）。真实样品 `chengdu-huagao-0801.glb` 验证：材质#1 三种 alphaMode 的 three.js 实例状态（transparent/depthWrite/alphaTest）逐项核对；doubleSided 切换用暴露的 `window.__debugTHREE.DoubleSide` 常量比对 `material.side` 数值真变化；材质#1 视觉验证换成无遮挡的材质#16（浅绿盒子）+ opacity=0.15 调试注入，OPAQUE/BLEND 截图对比确认真透明（透出地板网格线）而非只是数值层面变了；导出 GLB 解析 JSON chunk 确认 `materials[1].alphaMode==='BLEND'`/`.doubleSided===true`（原始样品两字段均为 `undefined`，隐式 OPAQUE 单面）；单独还原两字段确认回到原值；布局收紧截图跟 #7 阶段旧截图（`Doc/shots/02-detail-open.png`）对比确认更紧凑；控制台 0 报错。测试脚本：`_dev/test-alpha-doubleside.js`。实现记录见 `Doc/EDITOR-SPEC.md` §7。**同前几轮，harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch`（查询 "TaskUpdate TaskGet"）仍无匹配结果，只更新了这份文件**
- [x] **#19** 材质画廊工具栏：视图切换 + 贴图缩略图 + 视口取色 + 拖拽指定材质 —— 依赖 #15，2026-08-06 完成。**开工前先按要求搜了残留实现**（"视图切换"/`eyedropper`/"取色"/"拖拽指定"/`mat-toolbar`/`list-view` 等关键词）——`index.html` 里只有 `.mat-toolbar`（§5 材质清理菜单已经在用的工具条容器类）和视口原有「拖 GLB 文件进来加载」的 `dragover`/`drop` 监听器，没有任何跟这四个子功能相关的残留代码，确认是干净状态，从零实现，不是复用别的代理没做完的东西。四个子功能全部做完（不是按优先级只做一部分）：① 块视图/列表视图切换（模块级变量 `matGalleryView`，不持久化）；② 贴图缩略图（复用 #16 的 `getSlotTexture`/`textureThumbDataUrl`，没有另写解码逻辑）；③ 多色角标（`.swatch-badge` 小圆点露出自发光等非默认次色，块/列表视图都有）；④ 视口取色（`THREE.Raycaster` + `gltf.parser.associations` 反查 `raw.meshes[].primitives[].material`，`pointerdown`/`pointerup` 距离阈值判定「点击」而不是「转视角」，命中后画廊滚动定位+黄色闪烁+展开详情）+ 拖拽指定材质（HTML5 draggable，`dragover`/`drop` 复用视口既有监听器按 `draggedMatIndex` 分岔，写回 three.js `mesh.material`=`matInstances` 共享实例 + `raw.meshes[meshIndex].primitives[primIndex].material`，悬停时白色轮廓描边提示）。**已知范围限制**：多个节点原生共享同一个 `mesh` 索引（glTF instancing）时，拖拽只改被命中的那一个 `THREE.Mesh` 实例，不会连带改所有共享该 mesh 索引的其它节点——真实样品没有这种场景，没有为用不上验证的场景过度设计，留给以后有真实需求再扩展。真实样品 `chengdu-huagao-0801.glb`（17 材质，14 网格节点）验证：视图切换截图对比；材质 #1 上传测试图后画廊卡片精确显示 `<img class="swatch-img">`（不是纯色块）；材质 #0 改红色 baseColor + 蓝色 emissive 后角标立即出现（过程中发现并修复了一个真实 bug——emissive 编辑的 `oninput` 只顾着照抄 baseColor 那段 patch 色块背景，漏了同步 patch 角标，导致要等下次整体 `renderTab` 才补上，已修复成跟 patch 背景同一段代码里一起做）；取色用已知节点世界坐标投影到当前相机的屏幕坐标做真实 Playwright 鼠标点击（不是直接调内部函数），`selectedMat` 精确等于预测材质索引；拖拽把材质 #0 拖到取色定位到的同一网格上，`raw.meshes[13].primitives[0].material` 精确变成 0、命中 mesh 的 three.js `.material` 精确等于共享实例（不是新 clone）、视口截图肉眼确认外观真的从浅绿变红蓝、「另存为 GLB」导出后解析 JSON chunk 按节点名核对材质名字/`baseColorFactor` 精确匹配；控制台全程 0 报错；重跑 `test-highlight.js`/`test-mat-editor.js`/`test-texture-upload.js`/`test-scene-menu.js`/`test-node-detail-panel.js` 五个既有回归测试全部 PASS，确认没有破坏共用到的视口拖拽/全局 keydown 等既有代码路径。测试脚本：`_dev/test-gallery-toolbar.js`（Playwright，35 项断言全部 PASS，可重跑复查）。截图：`_dev/shots/gallery19-00` 至 `gallery19-07`。实现记录见 `Doc/EDITOR-SPEC.md` §6.3，README.md 表1材质小节新增「画廊工具栏」段落。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#20** 模型块表重构：上下分栏（选中详情 + 精简列表）—— 依赖 #15，2026-08-06 完成。**这次任务开头先按要求检查了有没有残留实现**——上一轮代理在中途撞到 API session 限额被打断，最后一句话是「All 45 assertions still pass. Now let's move on to documentation updates.」，搜索确认 `index.html` 里模型块 Tab 相关代码（`resolveNodeRowTarget`/`renderNodeDetail`/`node-detail-empty`/`node-merge-note`/`walk()` 合并逻辑等）已经完整存在，且 `_dev/index-before-todo20.html`（改前基准）、`_dev/test-node-detail-panel.js`（45 项断言测试脚本）、`_dev/gen-merge-test-gltf.js`（合成多子节点组测试文件生成器）、3 张截图（`_dev/shots/todo20-00/01/02/03`）都已产出——逐行读完代码（含 `resolveNodeRowTarget` 头部的合并挂载点判断逻辑长注释、`renderNodeDetail`/`walk()` 的树建行逻辑）判断质量合格、边界条件（未选中/找不到节点两种空态、多子节点组不合并、包围盒/移动旋转不走挂载点重定向）处理到位，**判断直接复用，不重做**，只重新完整跑了一遍测试脚本确认在当前代码状态下仍然正确工作。上方新增「选中节点详情」常驻区（`renderNodeDetail()`）：无选中显示提示不留空白/不报错；有选中展示属于/顶点三角/通道/材质色块/移动T/旋转R°/缩放S/相对基点坐标 + 四个操作按钮（⧉ Instance/⇩ 导出/⬚ 包围盒/✥ 移动旋转，从原来每行操作列整体挪过来，行为不变）；合并行额外显示 `.node-merge-note` 说明条（链路+挂载点+原因）。下方列表精简到 7 列（名称/材质/显示/允许/备注名称/备注/关联基点），去掉的属于/顶点三角/通道/T/R/S/相对基点这几项只挪到详情区，数据不丢。**单子节点组合并**（`walk()` DFS 建行时，纯包装组且唯一子节点直接递归跳过、depth 不增，直到遇到网格叶子或 0/≥2 子节点的组才落地成一行）——真实样品 28 节点（14 组+14叶）全部 14 组都是单子节点组，合并后精简到 14 行；多子节点组维持原展开方式不变（合成文件 `test-merge.glb` 验证 `BranchGroup` 挂 `LeafA`/`LeafB` 两个子节点正常各占一行）。**合并行注释挂载点判断**（`resolveNodeRowTarget()`）——默认挂链终点节点（这一行"实际代表"的东西），除非链上某层祖先包装组已有非默认注释内容（`nodeAnnoHasOverride()`），则改挂到最外层已有注释的那个祖先，避免用户已有标注被这次改版挪走或分裂成两条记录；包围盒/移动旋转这两类"针对具体节点"的操作不走这套重定向，始终对行终点节点本身生效。真实样品验证：合并行 `node_3 → L6` 选中后详情区标题/合并说明条/各字段正确，挂别名精确写入 `anno.nodes["L6"]`（不是 `node_3`，因为 `node_3` 当时无独立注释），导出注释 JSON 核实同一路径；合成文件验证 3 层链默认挂链终点、手动给最外层造注释后挂载点正确改到最外层，DOM 属性和输入框回填值同步验证。测试脚本 `_dev/test-node-detail-panel.js` 重跑 45 项断言全部 PASS（含改前/改后截图对比、真实样品全流程、合成文件多子节点组+3层链两个场景），控制台全程 0 报错；另重跑 `test-instance-export.js`/`test-bbox.js`/`test-basepoints.js`/`test-highlight.js`/`test-mat-editor.js` 确认没有破坏 Instance/包围盒/基点/高亮/材质编辑相关功能，全部通过。截图：`_dev/shots/todo20-00-before.png`（改前 15 列 28 行密集表格）/`todo20-01-after-empty.png`（改后空选中态 7 列 14 行）/`todo20-02-after-detail-open.png`（选中合并行，详情区+四按钮+视口包围盒线框）/`todo20-03-synthetic-multichild-and-chain.png`（合成文件多子节点组不合并+3层链合并共存）。数据结构层面 `Node`/`NodeAnnotation` 接口无变化，`SPEC.md` 不需要同步。实现记录见 `Doc/EDITOR-SPEC.md` §6.2，README.md 表3整节已重写。**同前几轮，harness 的 TaskUpdate 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#21** 头部菜单重构：移除示例模型 + 打开/另存为改下拉菜单 —— 2026-08-06 完成。**开工前先检查了有没有残留实现**——搜了「下拉菜单」「从服务器打开」「保存到服务器」等关键词，`index.html` 里只有 #12 场景菜单自己的实现，没有跟本任务相关的半成品代码，从零开始，没有可复用的半成品。先读了 `Doc/BACKEND-SPEC.md` 第六节的接口约定（`upload.php`/`projects.php` 字段名、成功/失败响应结构）和 #12 场景菜单的下拉交互实现，照抄不猜格式。①「示例模型」按钮（`#sampleBtn`）连同它的下载逻辑整个删除，不留占位。②「打开 GLB」改成下拉菜单：「本地文件」是原 `#openBtn` 行为原样保留；「从服务器打开」`GET server/api/projects.php` 拉列表弹面板，选中后 `GET ?id=` 拿详情+`fetch(glbUrl)` 拉 GLB 二进制，走**现有** `openBuf()`/`preprocess()` 流程渲染（注释 JSON 通过预写 `localStorage` 让 `openBuf()` 内部 `loadAnno()` 自然读到，不另写合并逻辑）。③「另存为 GLB」改成下拉菜单：GLTFExporter 打包逻辑抽成共用 `exportGlbBlob()`，「本地文件」原样保留，「保存到服务器」用同一个 blob + `buildExportJson()` 生成的注释 JSON 一起 multipart POST 给 `upload.php`（字段名 `glb`/`meta`/`name`）——**这次范围只做新建项目，不支持覆盖更新已上传的项目**（`upload.php` 本身也没有带 id 的更新接口）；上传成功后 `ownerToken` 用不会自动消失的浮层明显展示（不是状态栏一闪而过），提示「只显示这一次，请立即复制保管」，配复制按钮；token 只存会话内存变量 `currentServerProject`，不做 localStorage 持久化（跨会话丢失是任务明确允许的范围限制）。④ 三个下拉菜单（场景/打开GLB/另存为GLB）交互抽成通用 `registerMenu()`（原来 #12 场景菜单是单独手写一套开关函数+监听器，这次新增两个同款菜单后抽成共用基础设施，避免三份重复代码），行为跟 #12 原版逐条一致（点外部关闭/ESC关闭），额外补了「展开一个菜单收起其它菜单」。**架构决策**：本机测试和部署都改成用**同一个 PHP 内置服务器**（`php -S 127.0.0.1:<端口> -t .`，项目根目录不是 `server/`）同时托管静态页面和后端 API，前端相对路径 `fetch('server/api/...')` 天然同源，不需要处理 CORS，放弃了「Python 静态页面+PHP 后端两个独立进程」方案（那样需要后端自己加 CORS 头）。真实样品 `chengdu-huagao-0801.glb` 验证：截图确认按钮结构变化；本地打开材质数=17（回归）；UI 真实点击改材质 #0 颜色为 `#ff2020`；保存到服务器成功拿到 UUID 项目 ID + 64 位 token 并正确展示；**落盘核查**——`server/projects/<id>/` 下 `model.glb`（glTF magic bytes 校验通过）/`meta.json`（不含明文 token，只有哈希）/`versions/v1.3as.json`（材质颜色是编辑后的红色）都真实存在，curl 直接打 API 交叉核对内容一致；刷新页面（验证非本地缓存）后「从服务器打开」能看到项目、选中后完整加载出编辑后版本（材质数仍 17，颜色仍是服务器上的红色）；控制台全程 0 报错；共 41 项断言全部 PASS。测试脚本：`_dev/test-header-menu-backend.js`（Playwright，可重跑复查；测试产生的 `server/projects/<id>/` 已手动清理，`.gitignore` 规则用 `git add -n .` 交叉核对确认不会被意外提交）。截图：`_dev/shots/todo21-00` 至 `todo21-04`。文档同步：`README.md`（新增「头部菜单：打开 GLB / 另存为 GLB」独立小节 + 「快速开始」分层说明本地/后端两种运行方式）、`Doc/BACKEND-SPEC.md`（§四回填实际实现细节）、`Doc/EDITOR-SPEC.md`（§10 末尾新增实现记录）、`SPEC.md`（一处 `exportGlbBtn` 引用更新为现状）。**同前几轮，harness 的 TaskUpdate 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#22** 日志面板挪到底部状态栏 + 简单单步撤销按钮 —— 2026-08-06 完成。**开工前先按要求检查了有没有残留实现**——搜了「撤销」/`undo`/`pushUndo`/`undoStack`/`logBtn`/`logPanel`/`#status` 等关键词，`index.html` 里只有跟本任务无关的既有诊断日志系统（`#logBtn`/`#logPanel`/`logEntry()`，#14 已完成的独立功能）和各编辑功能已有的单字段「还原到原始值」按钮，没有任何跟撤销栈相关的残留代码，确认是干净状态，从零实现。**任务一**：`#status` 从单纯一行状态文字改成 flex 行布局——左侧 `#statusText`（状态文字，原 `status()` 函数改写这个而不是 `#status` 本身）+ 右侧「撤销」`#undoBtn` + 「日志」`#logBtn`（原头部独立按钮整个删除，ID 不变，只是父节点从 `<header>` 挪到 `#status`）。日志面板 `#logPanel` 展开位置从贴着头部按钮下方改成贴着状态栏上方，内容/交互（时间戳+级别标签+点行展开详情+清空/关闭）完全不变。**任务二**：通用 `pushUndo(label, undoFn)` + 8 步浅栈 + `performUndo()` + 状态栏按钮展示栈顶 label（栈空禁用），另加全局 Ctrl/Cmd+Z 快捷键（焦点在表单控件里时不触发，不劫持浏览器原生输入撤销）。按任务给的优先级顺序**全部六类编辑操作都接入完成**：① 材质字段编辑（baseColor/金属度/粗糙度/自发光/自发光强度/alphaMode/doubleSided）——拖拽类控件用「手势」粒度（`oninput` 只记手势开始前的值，`onchange` 松开时才提交撤销记录，避免栈被拖拽中间值刷满）；② 材质清理菜单批量操作——三个子函数（`cleanupRenameFallback`/`cleanupBlackToColor`/`cleanupNormalizeScale`）改成除了统计数字外也各自收集 `changes` 明细，`runMaterialCleanup` 合并成一条撤销记录，一次撤销把这次批量操作动过的全部材质/全部节点都精确恢复（不是只恢复一个）；③ 创建 Instance——利用新建节点在 `raw.nodes` 里是连续尾段的特性，撤销用 `raw.nodes.length` 截断 + 摘除父节点引用 + 清理场景图/关联表；④ 节点移动/旋转编辑——复用 `applyNodeWorldTransform` 同一条写回函数，撤销粒度是每个数字输入框单独一次失焦提交算一步；⑤ 贴图上传/替换/移除——写回前拍 raw 侧 mapDef + three.js 侧材质实例属性快照，撤销原样写回（不删除已写入 `raw.images`/`raw.textures` 的孤儿条目，跟移除贴图既有做法一致）；⑥ UV 变换——跟材质字段同一套「手势」粒度，复用 `setUvField`。**未接入撤销**（如实列出）：材质拖拽指定（#19）、包围盒生成/编辑/删除（#9）、测量基点新增/编辑/删除（#10）、场景整体包围盒手动覆盖开关、各处备注文字字段。跟已有「还原到原始值」按钮语义不同、互不冲突——那些还原到模型刚加载时的原始值，撤销回退的是「最近一步操作」。真实样品 `chengdu-huagao-0801.glb` 验证（`_dev/test-undo-status.js`，29 项断言全 PASS）：状态栏新布局截图确认、日志面板从新入口正常展开；材质 baseColor 编辑改红后撤销，three.js `Material.color` 与 `raw.materials[].pbrMetallicRoughness.baseColorFactor` 两边逐值核对精确恢复到编辑前（不是恢复成原始黑色，而是恢复到「这一步编辑之前」，这次场景下两者恰好相同）；材质清理菜单（重命名+缩放归一化，17 材质全部改名、14 节点缩放归一化）执行后撤销，17 个材质名字/颜色逐个深比较精确恢复、全部节点 `matrix`/`scale` 字段逐节点深比较精确恢复（含 matrix 表示的节点，不只是 scale 字段形式的）；连续两步编辑（先改粗糙度、再改金属度）确认撤销只回退最近一步（金属度），粗糙度的编辑保留，再撤销一次才回退粗糙度；控制台全程 0 报错。另重跑 `test-mat-editor.js`/`test-alpha-doubleside.js`/`test-uv-editor.js`/`test-texture-upload.js`/`test-node-transform.js`/`test-instance-export.js`/`test-cleanup-menu.js`/`test-highlight.js`/`test-gallery-toolbar.js` 九个既有回归测试，核心逻辑断言全部正常通过，确认没有破坏材质/UV/贴图/节点变换/Instance/清理菜单/高亮/拖拽指定材质等既有功能——**发现一个这几个测试脚本自己的预置问题（#21 遗留，不是这次引入的）**：它们导出 GLB 那一步还在用 #21 之前的旧按钮 ID `#exportGlbBtn`，#21 已经把它改成下拉菜单 `#saveGlbLocalBtn`，导致这几个脚本跑到导出步骤时超时崩溃（前面所有其它断言都已经正常通过打印），如实记录、这次任务没有顺手修（超出范围）。测试脚本：`_dev/test-undo-status.js`。截图：`_dev/shots/undo22-00` 至 `undo22-07`。实现记录见 `Doc/EDITOR-SPEC.md` §13 末尾。**同前几轮，harness 的 TaskUpdate 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#24** CSS 变量统一：选中高亮色并入 `:root` 变量体系（huashu-design 评审V2 发现）—— 2026-08-05 完成。`#39d6ff`/`#ff3fd6` 原来在 CSS `tr.node-sel` 规则和 JS `NODE_SEL_COLOR`/`MAT_HL_COLOR` 常量两处独立硬编码，改成 JS 数字常量（three.js Color 需要数字，是唯一权威来源）启动时通过 `setProperty()` 写入 `--select`/`--mat-hl` 这两个 CSS 自定义属性，CSS 规则改用 `var(--select)`；`:root` 里保留的初始值只是占位（会被 JS 覆盖，注释里标注了不要在这改）。截图验证视觉效果跟改之前完全一致，无回归。
- [x] **#25** 材质画廊空白处取消选择 + 高亮色统一改黄色系 + 材质高亮撞色兜底（§6.1 后续改进）—— 2026-08-05 完成。三件事：① `.mat-gallery` 容器新增 `onclick`，用 `e.target.closest('.mat-card')` 判断点的是卡片本体还是空白网格区，是空白区才清空 `selectedMat`+清高亮+收起详情面板（确认了现状：材质卡片原来没有「再点一次取消选中」的 toggle，这轮没有额外补，只按要求做了空白处取消）；② `NODE_SEL_COLOR`/`MAT_HL_COLOR` 从青色 `#39d6ff`/品红 `#ff3fd6` 统一改成同一个黄色 `#ffe600`（比另一候选 `#ffcc00` 跟 `--accent` 色相差更大、暗背景下辨识度更好），CSS 占位值和 `tr.node-sel` 里一处遗漏的硬编码 rgba 一并同步；③ 材质高亮撞色兜底用组合方案——法线外扩+背面剔除的纯白 `0xffffff` 轮廓描边（主力，唯一不依赖颜色对比的手段，每个命中网格独立 `ShaderMaterial`，描边宽度按各自 `boundingSphere` 自适应）+ 呼吸透明度动画（辅助，`anim()` 循环里周期性起伏 `matHighlightMaterial.opacity`）。过程中踩了一个 TDZ 坑：`anim()` 是立即执行的 IIFE，第一帧同步跑在 `matHighlightMaterial` 声明之前会报 `Cannot access before initialization`，把这个变量声明提前到 `anim()` 之前解决。验证：手写合成撞色 glTF（`_dev/gen-yellow-clash-gltf.js`，立方体材质 baseColor 用肉眼几乎分不出的 `#ffdd00`）+ 手写 PNG 解码/像素差异工具（`_dev/png-diff-utils.js`，纯 Node zlib）算出高亮前后 6.32% 像素发生明显变化，逐像素采样确认描边处存在真正的纯白 `(255,255,255)` 像素（跟叠加黄色/材质本身色相都能区分开）；撞色材质和深蓝对照材质的高亮可见度（像素变化占比）几乎完全一致，证明可见度不随材质撞色而衰减；真实样品 `chengdu-huagao-0801.glb` 材质 #1（细笔画文字网格）裁剪截图+像素采样同样验证到纯白描边像素；真实样品+合成场景两边都测了画廊空白处取消选择；导出 GLB 解析确认材质数值/材质数量/名单都未被高亮/描边污染；重跑 `test-highlight.js`/`test-mat-editor.js`/`test-uv-editor.js`/`test-instance-export.js` 四个既有回归测试全部 PASS，确认这轮改动没有引入回归；控制台全程 0 报错。测试脚本：`_dev/test-yellow-highlight.js`（可重跑复查）。实现记录见 `Doc/EDITOR-SPEC.md` §6.1「后续改进（TODO #25）」。**同前几轮，harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch`（查询 "TaskUpdate TaskGet"）仍无匹配结果，只更新了这份文件**
- [x] **#26** 材质/贴图大图预览弹窗 —— 2026-08-06 完成。**开工前先检查了有没有残留实现**——搜了「预览弹窗」/`lightbox`/「大图」/`previewOverlay`/`imgPreview`/`texPreview`/`zoomOverlay` 等关键词，`index.html` 里全部无匹配，确认是干净状态，从零实现。三处点击入口共用同一套居中浮层 `#texPreviewOverlay`/`#texPreviewPanel`（跟设置面板/包围盒面板同一套「居中浮层+点外部关闭」外壳）：① 材质画廊色块（`.swatch`，点击时顺带选中该材质，跟原有「点色块选材质」行为叠加而不是替换）；② 材质详情贴图槽缩略图（`renderUvSlot` 的 `.tex-thumb`）；③ 贴图 Tab 表格行新增的缩略图列（`texRowThumbHtml`，之前这张表完全没有缩略图，这次新增）。尺寸取舍：贴图长边 ≥800px 按 1:1 原始像素显示，不额外放大；长边 <800px 才等比放大到长边=800，给个合理最小可视尺寸，弹窗本身 `overflow:auto` 兜底防止大图撑爆屏幕。滚轮缩放纯 CSS `transform:scale()`（0.1×-8×），按需求原文不做拖拽平移。纯色材质（只有 `baseColor` 没有贴图）降级展示放大纯色块+文字提示「此材质没有贴图，仅有颜色 `#xxxxxx`」，不留空弹窗不报错。贴图 Tab 缩略图做了三级 fallback（有材质实时引用→复用已加载 `THREE.Texture` 免解码；`raw.images[i].uri` 是 data URI→直接用；孤儿贴图→现场从 BIN chunk 按 `bufferView` 解码，含 `currentBinOffset` 偏移，即任务背景提到的那个已修复的真 bug）。真实带贴图样品 `C:\Users\Lin\Desktop\Glb\画稿飞扬v2.glb`（20 材质、9 张真实贴图，未拷贝进仓库，用绝对路径引用）验证：画廊 7 张卡片贴图缩略图正常（确认 BIN 偏移 bug 修复生效）；点色块打开预览展示尺寸精确 2048×1152（长边本来就≥800，未做多余放大）；滚轮向上/向下缩放 `transform:scale()` 数值确实相应变大/变小；点弹窗外部关闭；材质详情贴图槽缩略图、贴图 Tab 表格行缩略图（9 行全部同步生成）两个入口各自验证打开成功。`chengdu-huagao-0801.glb`（17 材质 0 贴图）验证纯色降级：0 张卡片带贴图缩略图（确认样品真 0 贴图），点色块展示纯色块+「此材质没有贴图，仅有颜色 `#000000`」，点外部正常关闭。控制台全程 0 报错，共 20 项断言全部 PASS。**已知交互取舍（如实记录）**：色块点击同时承担「选中材质」+「打开预览」，块视图下色块占卡片绝大部分面积，点色块基本等于点卡片主体——只想选中不想弹预览可以点名字/索引文字区域（验证过不在色块范围内不触发预览）；这带来一个可预期的连带影响，之前 TODO（如 #19）的回归测试脚本里 `page.click('.mat-card[...]')`（点卡片几何中心，块视图下通常落在色块区域）会连带弹出预览弹窗挡住脚本后续点击——这是字面对齐本次任务需求（"点色块应该弹出大图预览"）的直接、可预期结果，不是这次实现的 bug，以后要重跑那些旧脚本需要相应调整点击目标。测试脚本：`_dev/test-preview-lightbox.js`（Playwright，20 项断言全部 PASS，可重跑复查）。截图：`_dev/shots/preview26-00` 至 `preview26-05`。文档同步：`README.md`（表1新增「大图预览弹窗」小节 + 表2新增缩略图字段说明）、`Doc/EDITOR-SPEC.md`（新增 §15）。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**

- [x] **#30** 测量基点 UI 补充：列表标注优先级来源 + 说明入口（对应 §8）—— 2026-08-06 完成，依赖 #10。**开工前先按要求检查了有没有残留实现**——上一轮代理中途撞到 API session 限额被打断，最后一句话只说到"TODO.md doesn't yet contain a #30 entry"，搜了「优先级」「basepoint」相关关键词，`index.html` 里全部只是 #10 已完成功能自身的既有代码（`computeDefaultBasepoint` 的日志文案、坐标系大段注释等），没有任何这次任务相关的半成品残留，确认是干净状态，正常开始实现。两件事：① 基点数据结构新增可选字段 `source`——`computeDefaultBasepoint()` 产出的自动基点带 `{kind:'auto', priority:1|2, detail}`（`addAutoBasepoint()` 复用同一份产出，天然带上）；`addBasepoint()` 手动新增的带 `{kind:'manual'}`；这次任务之前保存的旧注释 JSON 没有这个字段的基点，不回填、不去猜，统一按「手动创建」展示（`basepointSourceLabel()` 里的兜底分支，理由：反正只有「自动/非自动」两类可标，没有优先级信息硬编一个更危险）。场景 Tab 基点列表每行新增来源标签（`.bp-row` 内 `flex:0 0 100%` 独占一行，不用另包容器）：优先级 1/2 分别显示「默认（优先级1：GLB内Origin节点）」/「默认（优先级2：包围盒中心，未找到Origin节点）」+ 橙色边框徽标样式，手动创建显示「手动创建」+ 纯灰色文字不带醒目边框，两种视觉上一眼可辨。② 「测量基点」标题栏右侧新增 ⓘ 图标（`#bpHelpBtn`），点击弹出跟设置面板/材质清理菜单同一套「居中浮层」外壳（`#bpHelpOverlay`/`#bpHelpPanel`），内容是浓缩过的四句话说明（基点是干什么用的 + 三级优先级怎么运作 + 列表标签怎么看），没有照抄 `EDITOR-VIEWER-CONTRACT.md` 第五节给开发者看的技术性措辞（坐标系细节、字段名历史沿革等用户不需要关心）。真实样品 `chengdu-huagao-0801.glb`（无 Origin 节点）验证默认基点标注精确显示「默认（优先级2：包围盒中心，未找到Origin节点）」；复用 #10 阶段的合成 Origin 命名节点 glTF（`_dev/gen-origin-node-gltf.js`）验证命中优先级 1 时标注精确显示「默认（优先级1：GLB内Origin节点）」；点说明图标截图确认弹窗文字清楚（含「Origin」「包围盒」两个关键规则点，长度 293 字符，验证过是浓缩版本不是整段照抄），点外部背景关闭、点「关闭」按钮关闭两种方式都测到；新建手动基点确认标注「手动创建」+ CSS 类跟自动基点的 `auto` 类不同，UI 截图肉眼确认视觉区分明显（橙色边框徽标 vs 纯灰文字）；控制台全程 0 报错，共 21 项断言全部 PASS；重跑 `_dev/test-basepoints.js`（#10 阶段验证脚本）确认新增的 `source` 字段没有破坏原有基点增删改/视口标记/相对坐标计算/导出 JSON 功能，全部既有断言依然 PASS。测试脚本：`_dev/test-todo30-basepoint-source.js`（Playwright，可重跑复查）。截图：`_dev/shots/todo30-00` 至 `todo30-04`。`window.__debugBasepoints` 新增暴露 `basepointSourceLabel`。文档同步：`README.md`（「测量基点系统」小节新增「基点来源标注」段落）、`Doc/EDITOR-SPEC.md`（§8 末尾新增实现记录）。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**
- [x] **#29** 详情面板 T/R/S 内联可编辑 + 节点列表三行布局（含新字段「允许选中」/`atomicGroup`） + 去掉「合并」标签 + 拖拽重新挂靠父节点【最重要】—— 2026-08-06 完成。**开工前先按要求检查了有没有残留实现**——搜了「拖拽挂靠」/`reparent`/`allowSelect`/`draggable`/`dragstart` 等关键词，`index.html` 里除了材质画廊（#19）自己的拖拽指派材质逻辑（不相关）之外全部无匹配，确认是干净状态，这次是从零实现，没有可复用的半成品代码。四个任务全部完成：① T/R/S 内联编辑取代原来 #17 的独立弹窗（`#transformOverlay` 整个删掉），三组数字输入框横向并排在详情区同一行，复用 #17 写好的 `applyNodeWorldTransform` 核心写回逻辑（只加了一个可选的 `targetS` 第 4 参，没有重新推导矩阵数学），缩放 S 这次也从只读升级成可编辑；「相对基点坐标」判断维持只读（T 是权威数据，相对基点坐标是随 T 联动重算的只读派生视图，理由见 `Doc/EDITOR-SPEC.md` §16.1）。② 节点列表从 7 列表头改成「节点/信息」两列，信息列内部三行分组（材质 / 显示·允许编辑·允许选中 / 别名·关联基点·备注），新增的「允许选中」勾选框就是 `Doc/EDITOR-VIEWER-CONTRACT.md` 第三节「组锁定 `atomicGroup`」第一次有的真正 UI，语义互为反面（勾选＝`atomicGroup:false`），该文档第七节「待确认」第 1 条一并回填去掉待确认标记。③ 列表「⊂合并」标签、详情区 `.node-merge-note` 说明条整个删掉，合并挂载点重定向逻辑（`resolveNodeRowTarget`）一行代码没动，只是不再显式告诉用户"这是合并过的"。④ 拖拽重新挂靠：节点行名称格 `draggable="true"`（不是整个 `<tr>`，避免跟信息列的 checkbox/输入框打架），`dragover`/`drop` 挂在整行；写回 `raw.nodes[]`（复用 #6 创建 Instance 已经在用的"父节点 children[] 或 raw.scenes[i].nodes 顶层"寻址约定，这次多了"摘除"这一步）+ three.js 场景图（`newParentObj.attach(draggedObj)`）。**关键技术点（任务要求重点说明）**：查了 `vendor/three.module.js`，`.add()` 只改 `parent`/`children`，完全不碰 `position/quaternion/scale`，会导致局部坐标系被重新解读、世界位置跳变；`.attach()` 源码注释原话「maintaining the object's world transform」，内部自动用新旧父节点的 `matrixWorld` 反算局部矩阵，这次直接用它，没有自己手写矩阵反算。额外推导确认了 `.attach()` 走的 three.js 显示坐标系（比 raw 原生世界空间多套一层 `preprocess()` 归一化）算出来的局部变换写回 `raw.nodes` 仍然正确——外层归一化矩阵在"父节点世界矩阵的逆 × 目标世界矩阵"这个计算里精确抵消。防循环引用用 `canReparent()`（自己/自己的子孙都拒绝，`isDescendantOf()` 沿 `raw.nodes[].children` 递归判断，不依赖 three.js）。接入了单步撤销（#22，撤销时精确恢复操作前的原始 raw JSON 快照，不是再走一次 `.attach()` 反算）和操作脚本记录/重放系统（#13，新增 `reparentNode` op，`SPEC.md` `ScriptEntry.op` 联合类型同步）。**开发过程中真实踩到两个坑并修复**（详见 `Doc/EDITOR-SPEC.md` §16.1）：① 精度坑——真实样品有节点缩放小到 `6e-5` 量级，`.toFixed(4)` 显示会把未编辑字段的精度腰斩，改成"精确快照 + 判断字段有没有真的被改动"两段式提交；② 重入坑——"还原"按钮在提交时会触发整块 DOM 重建（连带自己），浏览器移除一个正获得焦点的元素会同步触发 blur/focusout，冒泡出一次重入调用用旧值覆盖刚写对的结果，加了共享重入标记 `nodeTrsBusy` 修复。真实样品 `chengdu-huagao-0801.glb` 验证：T/R/S 编辑改世界坐标后测试脚本独立重算（不调用被测函数，自己重新实现一遍沿 `raw.nodes` 父链累乘）确认误差 `0.000000`；三个勾选框（含新 `atomicGroup`）正常写回；拖拽两个互不相关的叶子节点（特意选新父节点自身缩放均匀的一对），拖拽前后独立重算世界坐标误差 `0.000000m`（位置/旋转/缩放全部精确保持），节点树层级确实变了（新父节点 `children[]` 精确包含被拖节点，原父节点正确摘除），导出注释 JSON / 「另存为 GLB」解析确认新父子关系正确写出；拖到自己子孙节点上被正确拒绝（`raw.nodes` 数据结构不变）；额外专项验证了"新父节点自身缩放不均匀"这个 three.js `.attach()` 文档自己写明的已知限制——位置依然精确保持（`0.000000m`），旋转确实出现偏差（符合预期，不是 bug）；控制台全程 0 报错。测试脚本：`_dev/test-todo29.js`（Playwright，43 项断言全部 PASS，可重跑复查，用手动构造 `DataTransfer`+派发 `DragEvent` 模拟拖拽，因为无头 Chromium 下原生鼠标事件序列不会可靠触发 HTML5 拖放事件）。重跑既有回归测试 `test-bbox.js`/`test-basepoints.js` 全部通过；`test-instance-export.js`/`test-highlight.js`/`test-mat-editor.js`/`test-undo-status.js` 撞上两个跟这次改动无关的既有已知问题（`#exportGlbBtn` 旧按钮 ID，#22 已记录的 #21 遗留问题；`#texPreviewOverlay` 拦截点击，#26 已记录的预期连带效应），命中这两个坑之前的断言全部正常通过，如实记录、这次没有顺手修（超出范围）。截图：`_dev/shots/todo29-01` 至 `todo29-07`。文档同步：`README.md`（表3模型块整节重写）、`SPEC.md`（`NodeAnnotation` 新增 `atomicGroup`，`ScriptEntry.op` 新增 `'reparentNode'`）、`Doc/EDITOR-VIEWER-CONTRACT.md`（第七节「待确认」第 1 条回填）、`Doc/EDITOR-SPEC.md`（新增 §16，四个子节详细记录）。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**

- [x] **#27** 材质编辑器重构：贴图槽合并进对应属性行 + 法线/AO折叠卷展栏 + 材质编辑面板只保留一个"还原"按钮（用户明确反馈现有布局"贴图槽和下方重复了"）—— 2026-08-06 完成。**开工前先按要求检查了有没有残留实现**——搜了「贴图槽合并」/「材质编辑器重构」等关键词，`index.html` 里全部无匹配（唯一命中的 `.mdRevertAll` 是 #7 阶段就有的「还原此材质」旧按钮，语义是"还原到原始值"不是这次要做的"按操作步数还原"，跟本次任务无关），确认是干净状态，从零实现。三件事：① 漫反射/金属度/粗糙度/自发光四个 flat 属性行末尾各加一个 🖼 贴图切换入口（`texToggleBtn`），点击就地展开该属性对应的 `renderUvSlot` 内容（上传/替换/移除+UV变换），金属度和粗糙度两个入口按钮共享同一份 `metallicRoughness` 展开状态和底层贴图槽（复用 #8 阶段"metalnessMap/roughnessMap 必须共享同一个 clone"那条已验证的坑规避，UI 拆两行不代表数据层拆两份）；② 法线/遮蔽 AO 两个没有 flat 属性可合并的槽位放进默认收起的 `<details class="mat-more-tex">` 折叠卷展栏；③ 材质详情面板顶部只留一个「⟲ 还原 (N)」按钮，取代原来每个字段/每个UV变换字段各自的还原按钮，N=0 时禁用。**"操作步数"具体怎么数**：`pushUndo(label, undoFn, meta)` 新增第三参 `meta`，材质相关的六类写回点（字段编辑/UV变换/贴图上传替换/贴图移除/alphaMode/doubleSided）都传 `{ matIndex: i }`，`materialUndoCount(i)` 数的是撤销栈里 `meta.matIndex === i` 的条目数——**没有按 label 里的材质名字文本匹配**，因为真实样品 `chengdu-huagao-0801.glb` 全部 17 个材质都叫同一个「fallback Material」，第一版按名字匹配的实现用这份样品测试时步数计数完全错乱，材质清理菜单等跨材质批量操作的撤销记录不传 `meta`，天然不计入任何单个材质的步数。**"还原此材质"具体怎么只撤销这个材质相关的部分**：`revertMaterialSteps(i)` 循环从撤销栈里筛出属于这个材质、当前还留着的条目，按时间倒序（从最新到最旧）依次 `splice` 摘除+执行 `undoFn()`，直到筛不出为止——不影响栈里其它材质/节点的条目。真实样品验证（`_dev/test-todo27-matpanel.js`，34 项断言全 PASS）：新布局截图（贴图入口内联、卷展栏默认收起）；展开贴图入口→上传/替换贴图→视口 three.js 实例确认生效；连续 5 步不同字段编辑（baseColor贴图/金属度/粗糙度/emissive/法线贴图），「还原 (N)」从 1 精确递增到 5；切到另一个材质独立编辑，两个材质步数互不干扰；点还原，该材质精确撤销全部步骤、步数归零、按钮禁用，另一个材质不受影响；#26 大图预览两个入口（画廊色块/详情贴图槽缩略图）确认未被破坏；控制台 0 报错。另用真实带贴图样品 `C:\Users\Lin\Desktop\Glb\画稿飞扬v2.glb`（20 材质 9 张真实贴图）验证已有贴图的槽位默认收起状态下入口按钮就带 `.has-tex` 圆点标记、展开后直接显示已有贴图和"替换贴图"文案（`_dev/test-todo27-realtex.js`，8 项断言全 PASS）。重跑五个既有回归测试全部修复到 PASS：`test-uv-editor.js`/`test-texture-upload.js`/`test-mat-editor.js`/`test-alpha-doubleside.js`/`test-undo-status.js`（改动原因如实记录在 `Doc/EDITOR-SPEC.md` §7 实现记录里，含顺手修复的 `#exportGlbBtn` 旧选择器等跟这次改动无关的既有遗留问题）；`test-highlight.js` 等其它受 #26 既有连带行为影响但跟这次材质编辑器改动无直接关系的脚本，如实记录同一问题、这次没有逐个都修。测试脚本：`_dev/test-todo27-matpanel.js`、`_dev/test-todo27-realtex.js`。截图：`_dev/shots/todo27-00` 至 `todo27-08`（`09` 没生成——真实带贴图样品里没有法线贴图材质，测试脚本正确走了跳过分支，见该分支内的 console.log）。文档同步：`README.md`（表1材质整节重写"贴图槽合并"部分 + 「简单单步撤销」节回填材质面板例外说明）、`Doc/EDITOR-SPEC.md`（§7 末尾新增实现记录）。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**

- [x] **#28** 四个 Tab（材质/贴图/模型块/场景）图标化 + 悬浮 tooltip（§2）—— 2026-08-06 完成。四个 Tab 按钮文字换成内联 `<svg>`（不引入外部图标字体/库，保持项目一贯的"纯前端单文件无构建步骤无外部依赖"）：材质→调色板+4色点、贴图→相片图标、模型块→等距立方体线稿、场景→四角取景器括号+中心点，四者形状刻意拉开区分度。图标线条统一用 `stroke="currentColor"`（少数填充点用 `fill="currentColor"`），不写任何单独的选中态配色 CSS——`button`/`button.on`/`button:hover` 三条既有规则本来就管 `color`，选中态金色、未选中态灰、hover 变亮全部免费继承。每个图标用浏览器原生 `title` 属性做悬浮 tooltip（值＝原来的中文文案），满足"必须有悬浮说明"这条硬性要求，没有另做自定义 tooltip 组件。`data-tab`/`renderTab()` 的 `.on` 类切换/`onclick` 绑定三处逻辑一个字没动，图标化只换了按钮内部可见内容。**顺带把 `#settingsBtn` 的 ⚙ emoji 也换成同风格 SVG 齿轮**——理由：emoji 字形不跟随 CSS `color`/hover 变化，换成线条图标后跟四个 Tab 视觉统一、交互反馈也统一了；风险低（`title`/ID/事件绑定都没动），直接做了不算是需要用户决定的取舍。真实样品 `chengdu-huagao-0801.glb` 验证：四个 tab 按钮均含 `<svg>` 且不再有裸文字、`title` 精确匹配中文名、默认材质 tab 选中态正确、切换到其它三个 tab 时 `.on` 精确跟随切换（每次仅一个）且各自 `#tables` 渲染正常（点击切换功能未受影响）；`settingsBtn` 换图标后点击仍能正常打开设置面板；选中态/未选中态两个按钮的 computed `color` 值实测不同（视觉区分有量化证据，非目测）；控制台全程 0 报错，共 21 项断言全部 PASS。测试脚本：`_dev/test-todo28-tab-icons.js`（Playwright，可重跑复查）。截图：`_dev/shots/todo28-00` 至 `todo28-07`。实现记录见 `Doc/EDITOR-SPEC.md` §2。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**

- [x] **视口工具：视角快捷按钮**（用户 2026-08-06 在 #27 收尾阶段中途补充的口头要求，非事先写进本文档的编号任务，参考对象是 `C:\Users\Lin\projects\3ds-viewer` 最新版左上角的 `#views` 小工具条）—— 2026-08-06 完成。3AS 视口原来没有任何视角快捷入口（只能靠鼠标手动拖拽 OrbitControls），3ds-viewer 那边视口左上角有一个竖排小工具条（`position:absolute; top:58px; left:12px`，正视/顶视两个按钮，点击相机飞到标准视角并框住模型，按钮高亮当前视角，手动拖拽后高亮清除）——这次给 3AS 视口做了一个同位置/同交互模式的对应物，配色/尺寸照抄本项目已有的暗色变量体系，不引入 3ds-viewer 的配色。**不是照搬 3ds-viewer 的具体数值**：3ds-viewer 是 Z-up 场景，3AS 是 Y-up（three.js 默认），方向向量按 3AS 自己的坐标系重新推导，不是抄一份改坐标名字；3ds-viewer 那份左上角工具条本身只有正视/顶视 2 个按钮（侧视等其它视角要么在别处触发要么还没做），这次给 3AS 做了 4 个（正视/侧视/顶视/默认），比参考对象更完整一些，判断这对一个标注类工具更有用（用户经常需要从任意一侧检查标注是否贴对位置）。**没有搬 3ds-viewer 那份 2026-08-06 的「灯光/操作轴/三视图测量网格/加载动画」需求文档**（`3ds-viewer/doc/2026-08-06-huagao3d-灯光-轴向-视图-测量-加载动画需求.md`）——那份文档本身状态是"需求整理完成，尚未修改代码"（Q1-Q5 待用户确认），3ds-viewer 自己都还没实现，没有东西可"参考"，且用户这次只提了"视口工具"，没有提测量网格/灯光滑块这些，不在这次范围内。四个按钮：正视（沿 +Z 看向 -Z）/侧视（沿 +X 看向 -X）/顶视（正上方俯视，`camera.up` 从默认 `(0,1,0)` 改成 `(0,0,-1)`，否则跟视线方向平行会退化）/默认（跟 `preprocess()` 里模型刚加载完那一刻的初始机位公式同一个方向，只是按"当前"包围盒重新算距离——创建 Instance/节点变换等操作可能已经改变了包围盒，不能用加载那一刻算好就存死的旧值）。距离计算用包围球同时满足垂直/水平视场角的下限（参考 3ds-viewer `frameActiveScene()` 的算法思路，不是随手估的系数）。模型加载前 `#viewTools` 是 `hidden`（没有模型点了也无意义），加载完成那一刻跟 `#dropHint` 隐藏同一行代码一起显示。真实样品 `chengdu-huagao-0801.glb` 验证：4 个按钮全部正确渲染+点击生效（读 `camera.position`/`controls.target`/`camera.up` 精确核对每个视角的方向/up向量）；点击后按钮高亮精确对应当前 `activeViewportPreset`；手动拖拽视口（模拟鼠标 down-move-up）后 `activeViewportPreset` 清空、按钮高亮全部消失；截图确认正视/侧视两个角度视觉上确实是绕着模型转了 90°（不是数值对但视觉没变的假生效）；控制台全程 0 报错，共 21 项断言全部 PASS（过程中一次因为 headless Chromium 瞬时 WebGL 上下文问题导致视口画面空白重跑一次后消失，这是 EDITOR-SPEC.md §7 之前就记录过的同一个 Playwright/SwiftShader 环境抖动，不是代码问题，同一状态重复验证多次数据链路本身没有出过错）。测试脚本：`_dev/test-viewport-tools.js`。调试钩子：`window.__debugViewport = { camera, controls, activeViewportPreset, frameViewportPreset }`。文档同步：本条（`Doc/TODO.md`，追加在 #28 之后，不占用编号）。**同前几轮，harness 的 `TaskUpdate` 工具本轮 `ToolSearch`（查询 "TaskUpdate"）仍无匹配结果，只更新了这份文件**

**排期建议**：#2（另存为 GLB）应该尽早做，因为后面所有"编辑并写回"类功能（#4/#7/#8/#9）做完都需要它才能真正落地导出，不然都只是内存里好看。#5（树状节点表）是 #6/#10 的前置依赖，也建议提前。#13 依赖最多，理所当然放最后。#14（日志系统）没有强依赖，随时可以插空做，越早做越方便后面调试其他功能。

---

## 3AS 后端（对应设计文档 [`BACKEND-SPEC.md`](./BACKEND-SPEC.md)）

- [x] **#23** GLB 上传与存储 + 多项目管理（BACKEND-SPEC.md §2.1 + §2.2）—— 2026-08-05 完成。新建 `server/api/upload.php`（multipart 上传 GLB + 可选注释 JSON，glTF magic bytes 校验，UUID 项目 ID，owner token 只存 sha256 哈希、明文仅响应一次返回）+ `server/api/projects.php`（GET 列表/单个、DELETE 带 token 校验，ID 全部走正则白名单防路径穿越）。本机 `php -S 127.0.0.1:18280 -t server/` 端到端测试通过（用真实样品 `chengdu-huagao-0801.glb`）：上传→列表→详情→假 token 删除被拒 403→真 token 删除成功→假 GLB 文件被 415 拒绝→`../` 路径穿越 id 被 400 拒绝，共 13 项检查全通过；另外单独验证了代码层 200MB 大小上限（独立于 php.ini 配置，用 220MB 假文件 + 放宽 ini 上限的第二个测试服务器验证仍被拒绝）。测试脚本：`_dev/test-backend.sh`（可重跑复查）。实现记录见 `Doc/BACKEND-SPEC.md` 末尾。**同 EDITOR-SPEC 那几轮一样，harness 的 TaskUpdate/TaskGet 工具本轮 `ToolSearch` 仍搜不到（只返回 TaskStop/SendMessage/Monitor），只更新了这份文件**
